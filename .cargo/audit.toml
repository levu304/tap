[advisories]
ignore = [
    # RUSTSEC-2024-0436: paste is unmaintained, transitive via thiserror v1.
    "RUSTSEC-2024-0436",
    # RUSTSEC-2025-0020: PyString::from_object buffer overflow in pyo3.
    # Scaffold stubs don't call this API. Upgrade when v0.24+ is available.
    "RUSTSEC-2025-0020",
    # RUSTSEC-2025-0057: fxhash is unmaintained, transitive via napi-rs.
    "RUSTSEC-2025-0057",
    # RUSTSEC-2026-0086: wasmtime Winch 64-bit table data leakage. We use Cranelift.
    "RUSTSEC-2026-0086",
    # RUSTSEC-2026-0088: wasmtime pooling allocator. We use per-instance mmap.
    "RUSTSEC-2026-0088",
    # RUSTSEC-2026-0089: wasmtime Winch `table.fill` host panic. N/A (Cranelift only).
    "RUSTSEC-2026-0089",
    # RUSTSEC-2026-0094: wasmtime Winch `table.grow`. N/A (Cranelift only).
    "RUSTSEC-2026-0094",
    # RUSTSEC-2026-0095: wasmtime Winch sandbox escape. N/A (Cranelift only).
    "RUSTSEC-2026-0095",
    # RUSTSEC-2026-0096: wasmtime aarch64 Cranelift heap access miscompilation.
    # Affects < 25.0.1 on aarch64. CI runs on x86_64; upgrade to v25+ when available.
    "RUSTSEC-2026-0096",
    # RUSTSEC-2026-0173: proc-macro-error2 is unmaintained.
    # Transitive via mysql_async (mysql-common-derive). No safe upgrade available.
    "RUSTSEC-2026-0173",
    # RUSTSEC-2026-0176: Out-of-bounds read in pyo3 PyList/PyTuple iterators.
    # RUSTSEC-2026-0177: Missing Sync bound on pyo3 CFunction closures.
    # Fix: upgrade pyo3 from 0.24 to >=0.29. Blocked by pre-1.0 API churn
    # in tap-sdk-py. Evaluate after crate stabilization.
    "RUSTSEC-2026-0176",
    "RUSTSEC-2026-0177",
    # RUSTSEC-2026-0178: Panic on DataRow field mismatch in tokio-postgres.
    # RUSTSEC-2026-0179: Unbounded SCRAM iteration DoS in postgres-protocol.
    # RUSTSEC-2026-0180: Panic on malformed hstore in postgres-protocol.
    # Fix: postgres-protocol 0.6.12, tokio-postgres 0.7.18.
    # CI generates fresh Cargo.lock but Swatinem/rust-cache may restore old lock.
    "RUSTSEC-2026-0178",
    "RUSTSEC-2026-0179",
    "RUSTSEC-2026-0180",
]
