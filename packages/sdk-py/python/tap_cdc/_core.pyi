"""Type stubs for the native `tap_cdc._core` module."""

__version__: str
"""Package version (mirrors crate version)."""

class TapCore:
    """Internal Rust capture engine.

    Stub — methods will be added in Phase 2 when real capture
    functionality is exposed to Python consumers.
    """
    def __init__(self) -> None: ...
